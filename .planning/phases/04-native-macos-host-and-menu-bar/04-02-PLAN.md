---
phase: 04-native-macos-host-and-menu-bar
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - macos/Goto/MenuBar/GotoMenuBarExtra.swift
  - macos/Goto/MenuBar/ProjectMenuView.swift
  - macos/Goto/MenuBar/MenuBarViewModel.swift
  - macos/GotoTests/MenuBarViewModelTests.swift
autonomous: true
requirements: [MB-01, MB-04]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
must_haves:
  truths:
    - The menu bar surface renders the saved project list without opening a shell first.
    - Broken paths are visible or disabled instead of silently failing.
  artifacts:
    - macos/Goto/MenuBar/GotoMenuBarExtra.swift
    - macos/Goto/MenuBar/ProjectMenuView.swift
    - macos/Goto/MenuBar/MenuBarViewModel.swift
    - macos/GotoTests/MenuBarViewModelTests.swift
  key_links:
    - the menu bar consumes the shared registry model from plan 04-01
    - missing-path state is represented in the menu before launch is attempted
---

<objective>
Build the menu bar project list and surface states.

Purpose: make saved projects reachable from the macOS menu bar before Terminal automation is added.
Output: a working menu bar extra that renders saved projects, refreshes from the registry, and visibly distinguishes broken entries.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-CONTEXT.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-RESEARCH.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-VALIDATION.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the SwiftUI menu bar extra shell</name>
  <files>macos/Goto/MenuBar/GotoMenuBarExtra.swift, macos/Goto/MenuBar/ProjectMenuView.swift</files>
  <action>Build the initial `MenuBarExtra` surface that opens from the macOS menu bar and renders the saved project list. Keep selection actions inert for now if needed; the focus of this plan is rendering, grouping, and state clarity.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>The app shows a menu bar entry that can render the saved projects list from native code.</done>
</task>

<task type="auto">
  <name>Task 2: Add refresh and missing-path state handling</name>
  <files>macos/Goto/MenuBar/MenuBarViewModel.swift, macos/Goto/MenuBar/ProjectMenuView.swift</files>
  <action>Implement a small view model or equivalent state layer that loads projects on appearance, supports manual refresh or predictable reload timing, and marks missing entries clearly in the menu. Do not let missing paths look selectable without consequence.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The menu bar stays in sync with the registry and broken paths are visible or disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for menu rendering state</name>
  <files>macos/GotoTests/MenuBarViewModelTests.swift</files>
  <action>Add tests for list ordering, empty-state behavior, and missing-path representation in the menu model. Keep the tests focused on state and derivation, not brittle UI snapshots.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>Native tests protect the menu bar list behavior before launch actions are introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] Manual smoke pass confirms the menu bar extra appears and shows saved projects
- [ ] Broken paths render with an explicit missing or disabled state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The menu bar surface works as a project launcher shell before Terminal automation is added
- Missing-path handling is visible in the UI rather than deferred to runtime failure
</success_criteria>

<output>
After completion, create `.planning/phases/04-native-macos-host-and-menu-bar/04-02-SUMMARY.md`
</output>
