---
phase: 04-native-macos-host-and-menu-bar
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - macos/Goto/Core/TerminalLauncher.swift
  - macos/Goto/Core/TerminalPermissionState.swift
  - macos/Goto/MenuBar/ProjectLaunchAction.swift
  - macos/GotoTests/TerminalLauncherTests.swift
  - README.md
autonomous: true
requirements: [MB-02, MB-03]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
  - Allow Terminal automation when prompted during manual validation
must_haves:
  truths:
    - Selecting a project from the menu bar opens Terminal at the correct directory.
    - If Terminal is already open, the launch flow reuses that active Terminal context rather than creating a broken second path.
    - Automation failures surface clear user-facing feedback.
  artifacts:
    - macos/Goto/Core/TerminalLauncher.swift
    - macos/Goto/Core/TerminalPermissionState.swift
    - macos/Goto/MenuBar/ProjectLaunchAction.swift
    - macos/GotoTests/TerminalLauncherTests.swift
    - README.md
  key_links:
    - the menu bar uses one shared Terminal launcher
    - permission-denied behavior is explicit instead of silent
---

<objective>
Finish the menu bar launcher by wiring project selection into Terminal.

Purpose: convert the menu bar list from a passive surface into the requested launch behavior for saved projects.
Output: a reusable Terminal launch bridge, permission-aware failure handling, and menu bar actions that open or reuse Terminal correctly.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-CONTEXT.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-RESEARCH.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-VALIDATION.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-01-PLAN.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the shared Terminal launch bridge</name>
  <files>macos/Goto/Core/TerminalLauncher.swift, macos/Goto/Core/TerminalPermissionState.swift</files>
  <action>Implement one native launch path that can open Terminal at a given directory, detect the relevant permission or automation state, and report clear outcomes back to the caller. Keep the API reusable so Finder can call the same launcher in Phase 5.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The repository has a single reusable Terminal launch bridge with explicit success and failure states.</done>
</task>

<task type="auto">
  <name>Task 2: Wire menu bar selection into the launch bridge</name>
  <files>macos/Goto/MenuBar/ProjectLaunchAction.swift, macos/Goto/MenuBar/ProjectMenuView.swift</files>
  <action>Connect project selection in the menu bar to the shared launch bridge. Ensure missing paths are blocked before launch, and expose failure copy for permission denial or launch errors instead of silently doing nothing.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>Selecting a valid project from the menu bar attempts the shared Terminal launch flow and surfaces failures clearly.</done>
</task>

<task type="auto">
  <name>Task 3: Add launch-focused tests and documentation</name>
  <files>macos/GotoTests/TerminalLauncherTests.swift, README.md</files>
  <action>Add tests for launch request construction, permission-state handling, and broken-path rejection. Update the README with native prerequisites and the manual validation expectations for Terminal automation so the first-run experience is not surprising.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The launch bridge has automated coverage where feasible, and the repository documents the native permission requirement.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] Manual smoke pass confirms launch when Terminal is closed
- [ ] Manual smoke pass confirms launch reuse when Terminal is already open
- [ ] Manual smoke pass confirms permission denial shows clear feedback
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The menu bar surface satisfies the requested saved-project launch behavior
- Finder can reuse the same launch bridge in Phase 5 without duplicating launch logic
</success_criteria>

<output>
After completion, create `.planning/phases/04-native-macos-host-and-menu-bar/04-03-SUMMARY.md`
</output>
