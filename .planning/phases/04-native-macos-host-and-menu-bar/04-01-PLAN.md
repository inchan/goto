---
phase: 04-native-macos-host-and-menu-bar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - macos/Goto.xcodeproj/project.pbxproj
  - macos/Goto/App/GotoApp.swift
  - macos/Goto/Core/ProjectEntry.swift
  - macos/Goto/Core/RegistryStore.swift
  - macos/GotoTests/RegistryStoreTests.swift
autonomous: true
requirements: [APP-01]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
must_haves:
  truths:
    - A native macOS host exists for later menu bar and Finder work.
    - The native code can read the same `~/.goto` registry the CLI uses.
    - Registry parsing semantics match the existing shell product closely enough to avoid drift.
  artifacts:
    - macos/Goto.xcodeproj/project.pbxproj
    - macos/Goto/App/GotoApp.swift
    - macos/Goto/Core/ProjectEntry.swift
    - macos/Goto/Core/RegistryStore.swift
    - macos/GotoTests/RegistryStoreTests.swift
  key_links:
    - the native registry reader points at `~/.goto`
    - the app target compiles before any menu bar launch behavior is added
---

<objective>
Create the native macOS foundation for `goto`.

Purpose: establish a buildable app target and shared registry reader before any menu bar or Terminal automation work begins.
Output: a native app shell, shared project-entry model, and automated coverage for registry reading semantics.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BASELINE_AUDIT.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-CONTEXT.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-RESEARCH.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the native macOS app target</name>
  <files>macos/Goto.xcodeproj/project.pbxproj, macos/Goto/App/GotoApp.swift</files>
  <action>Create the initial Xcode project or workspace for a macOS app target named `Goto`. Keep the shell intentionally thin: app launch, menu bar hosting entry point, and room for a shared core module. Do not wire Finder or Terminal automation yet.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>The repository contains a buildable native host target for later menu bar work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared project-entry and registry-reading models</name>
  <files>macos/Goto/Core/ProjectEntry.swift, macos/Goto/Core/RegistryStore.swift</files>
  <action>Implement native models that read `~/.goto`, preserve stable ordering, trim blank lines, and derive the same high-level project information the shell picker uses. Surface missing-path state in the model so the UI does not have to invent it later.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>Native tests prove the shared registry reader can parse saved projects and detect missing paths without diverging from the CLI contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add native tests for registry semantics</name>
  <files>macos/GotoTests/RegistryStoreTests.swift</files>
  <action>Add tests that cover file parsing, ordering, blank-line trimming, and missing-path detection. Use temp directories and temp files so the native tests stay hermetic and do not touch the real user registry.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The native core has automated coverage for the shared registry contract before any launch UI depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] Native registry tests cover ordering, blank lines, and missing-path detection
- [ ] The native app target exists without Finder or Terminal automation scope creep
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a native macOS host that can become the shared base for menu bar and Finder surfaces
- Registry semantics are shared by contract rather than left implicit
</success_criteria>

<output>
After completion, create `.planning/phases/04-native-macos-host-and-menu-bar/04-01-SUMMARY.md`
</output>
