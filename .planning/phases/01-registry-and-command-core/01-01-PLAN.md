---
phase: 01-registry-and-command-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, bin/goto.js, src/cli.js, src/output.js, test/cli-contract.test.js]
autonomous: true
requirements: [INST-01]
user_setup: []
must_haves:
  truths:
    - The repository exposes a runnable `goto` CLI entrypoint for local development.
    - The CLI contract cleanly separates success/error signaling and is ready for later shell integration.
  artifacts:
    - package.json
    - bin/goto.js
    - src/cli.js
    - test/cli-contract.test.js
  key_links:
    - package.json maps the `goto` bin to bin/goto.js
    - bin/goto.js delegates argument handling to src/cli.js
---

<objective>
Create the local CLI foundation for `goto`.

Purpose: Lock the command contract before registry behavior and shell integration build on top of it.
Output: A runnable Node-based CLI entrypoint with contract tests and predictable exit behavior.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registry-and-command-core/01-CONTEXT.md
@.planning/phases/01-registry-and-command-core/01-RESEARCH.md
@.planning/phases/01-registry-and-command-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the package and executable entrypoint</name>
  <files>package.json, bin/goto.js</files>
  <action>Create a minimal buildless Node package for the project. Add a `bin` mapping for `goto`, make the entrypoint executable, and keep the initial command surface intentionally small: help text, argument capture, and a clear dispatch path for future `select`, `add`, and `remove` flows. Avoid pulling in extra CLI frameworks because this phase only needs a narrow contract.</action>
  <verify>node bin/goto.js --help</verify>
  <done>`node bin/goto.js --help` prints stable usage text and exits successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Define the CLI/output contract in reusable modules</name>
  <files>src/cli.js, src/output.js, bin/goto.js</files>
  <action>Move parsing and output behavior into small reusable modules. Explicitly define exit codes and output channels so future shell wrappers can rely on them. Human-readable errors should be consistent, and machine-readable stdout should stay reserved for later selection output instead of ad hoc logging. Lock these semantics in code comments or tests so later plans do not have to infer them.</action>
  <verify>node bin/goto.js --unknown-option</verify>
  <done>Unknown arguments fail predictably with a non-zero exit, readable guidance, and no accidental machine-readable stdout.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests for the CLI surface</name>
  <files>test/cli-contract.test.js</files>
  <action>Add automated tests that cover help output, invalid argument handling, explicit exit code expectations, and stdout/stderr separation for the initial command contract. Use the built-in Node test runner so the project stays buildless and phase validation remains fast.</action>
  <verify>node --test test/cli-contract.test.js</verify>
  <done>The CLI contract test file passes and protects exit codes plus output-channel behavior from drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test test/cli-contract.test.js`
- [ ] `node bin/goto.js --help`
- [ ] `node bin/goto.js --unknown-option` exits non-zero with readable output
- [ ] CLI contract tests assert stdout/stderr routing and exit codes for help and invalid-argument paths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a stable `goto` entrypoint for later plans to build on
- Exit-code and output-channel semantics are explicit enough for Phase 2 shell wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/01-registry-and-command-core/01-01-SUMMARY.md`
</output>
