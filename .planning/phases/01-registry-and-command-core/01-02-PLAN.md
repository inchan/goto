---
phase: 01-registry-and-command-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/paths.js, src/registry.js, src/commands/add.js, src/commands/remove.js, src/cli.js, test/registry.test.js, test/command-mutations.test.js]
autonomous: true
requirements: [REG-01, REG-02, REG-03, REG-04, REG-05, REG-06]
user_setup: []
must_haves:
  truths:
    - `goto -a` and `goto -r` work for the current directory and explicit paths.
    - The registry stores canonical absolute paths and refuses duplicates or invalid directories.
    - Registry mutations are durable and safe across repeated terminal use.
  artifacts:
    - src/paths.js
    - src/registry.js
    - src/commands/add.js
    - src/commands/remove.js
    - test/registry.test.js
    - test/command-mutations.test.js
  key_links:
    - src/cli.js dispatches add/remove requests into the dedicated command modules
    - command modules use shared path normalization and registry persistence helpers
---

<objective>
Implement the registry core and add/remove command behavior.

Purpose: Deliver the actual saved-project mutation logic that makes `goto` more than a placeholder entrypoint.
Output: Canonical path handling, atomic registry persistence, and automated coverage for add/remove flows.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registry-and-command-core/01-CONTEXT.md
@.planning/phases/01-registry-and-command-core/01-RESEARCH.md
@.planning/phases/01-registry-and-command-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build path normalization and registry persistence helpers</name>
  <files>src/paths.js, src/registry.js</files>
  <action>Create small utilities that resolve candidate paths into canonical absolute directories and persist them to `~/.goto` using atomic rewrite semantics. Treat malformed or missing directories as errors, not partial successes. Keep the registry format line-based and human-inspectable.</action>
  <verify>node --test test/registry.test.js</verify>
  <done>Tests prove canonicalization, dedupe behavior, and safe registry reads/writes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement add/remove commands against the registry</name>
  <files>src/commands/add.js, src/commands/remove.js, src/cli.js</files>
  <action>Implement `goto -a`, `goto -a PATH`, `goto -r`, and `goto -r PATH` with clear success, no-op, and error messaging. Default missing path arguments to the current working directory. Make sure the command behavior stays deterministic for later shell-wrapper usage, including explicit exit codes and predictable stdout/stderr behavior for success, duplicate/no-op, and invalid-path outcomes.</action>
  <verify>tmp_home=$(mktemp -d) && HOME=\"$tmp_home\" node bin/goto.js -a \"$PWD\"</verify>
  <done>The CLI can add or remove entries in a temp registry without touching the real user home.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for registry mutation workflows</name>
  <files>test/registry.test.js, test/command-mutations.test.js</files>
  <action>Add end-to-end tests that exercise current-directory defaults, explicit-path mutations, duplicate rejection, invalid-path failures, and the contract around exit codes plus stdout/stderr routing. Use temporary directories and temporary `HOME` overrides so repeated runs are hermetic.</action>
  <verify>node --test test/registry.test.js test/command-mutations.test.js</verify>
  <done>The registry mutation tests cover all Phase 1 registry requirements, including stream and exit-code behavior, and pass reliably.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test test/registry.test.js test/command-mutations.test.js`
- [ ] `tmp_home=$(mktemp -d) && HOME="$tmp_home" node bin/goto.js -a "$PWD"`
- [ ] `tmp_home=$(mktemp -d) && HOME="$tmp_home" node bin/goto.js -r "$PWD"` produces a clear no-op or success result
- [ ] Tests assert exit codes and stdout/stderr behavior for success, duplicate/no-op, and invalid-path cases
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Registry mutations are correct, canonical, and safe to build Phase 2 on top of
- Future shell wrappers can rely on deterministic add/remove command semantics
</success_criteria>

<output>
After completion, create `.planning/phases/01-registry-and-command-core/01-02-SUMMARY.md`
</output>
