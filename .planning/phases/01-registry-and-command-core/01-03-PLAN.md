---
phase: 01-registry-and-command-core
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [README.md, package.json, test/install-smoke.test.js]
autonomous: true
requirements: [INST-01]
user_setup: []
must_haves:
  truths:
    - A developer can follow the repository's documented local-install path and run the Phase 1 CLI successfully.
    - The documented install path matches the actual package/bin configuration.
  artifacts:
    - README.md
    - test/install-smoke.test.js
    - package.json
  key_links:
    - README.md setup steps match the package.json bin configuration
    - install smoke tests exercise the same local execution path documented for users
---

<objective>
Ship the repo-local install path and smoke verification for the command-core milestone.

Purpose: Make the Phase 1 deliverable usable from the repository and prove the setup path is real, not implied.
Output: Local install documentation, any required package metadata adjustments, and smoke checks that validate the documented flow.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registry-and-command-core/01-CONTEXT.md
@.planning/phases/01-registry-and-command-core/01-RESEARCH.md
@.planning/phases/01-registry-and-command-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the repo-local install and Phase 1 usage flow</name>
  <files>README.md</files>
  <action>Write concise setup instructions for running the project locally from this repository with a hermetic path that can be proven in automation. Document `npm install` followed by `npm exec goto -- ...` as the Phase 1 install-and-run flow. Cover only the intended Phase 1 usage surface: add/remove commands, registry file location, and the fact that shell-jump behavior arrives in Phase 2.</action>
  <verify>rg -n \"npm exec goto|goto -a|goto -r|~/.goto\" README.md</verify>
  <done>The README gives a concrete Phase 1 install path and usage examples that match the actual implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke checks for the documented local-install path</name>
  <files>test/install-smoke.test.js, package.json</files>
  <action>Add an automated smoke check that validates the package/bin metadata and exercises the CLI through the exact documented local path: `npm install` plus `npm exec goto -- ...`. Use temp-home execution and package metadata validation so the proof is hermetic and does not rely on machine-global state.</action>
  <verify>node --test test/install-smoke.test.js</verify>
  <done>The project has automated coverage that proves the documented local-install path produces a runnable `goto` command.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile docs and command behavior into a release-ready Phase 1 slice</name>
  <files>README.md, package.json</files>
  <action>Review the implemented command-core behavior against the docs and tighten any mismatches in copy, script names, or metadata. Avoid leaking future Phase 2 shell-jump promises into the Phase 1 instructions; Phase 1 should clearly be the registry-command milestone.</action>
  <verify>npm pack --dry-run</verify>
  <done>The documented install path, package metadata, and smoke verification all agree with the delivered Phase 1 command surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test test/install-smoke.test.js`
- [ ] `npm pack --dry-run`
- [ ] `npm install && HOME="$(mktemp -d)" npm exec goto -- --help`
- [ ] `rg -n "npm exec goto|goto -a|goto -r|~/.goto" README.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo-local install path is explicit, real, and protected by smoke checks
- INST-01 is proven through the same install path the README tells users to follow
</success_criteria>

<output>
After completion, create `.planning/phases/01-registry-and-command-core/01-03-SUMMARY.md`
</output>
