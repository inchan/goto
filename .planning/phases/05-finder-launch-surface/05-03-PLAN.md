---
phase: 05-finder-launch-surface
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - macos/GotoFinderAction/ActionRequestHandler.swift
  - macos/GotoFinderAction/FinderErrorPresenter.swift
  - README.md
  - macos/GotoTests/FinderErrorPresenterTests.swift
autonomous: true
requirements: [FDR-01, FDR-03, FDR-04]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
  - Allow Terminal automation when prompted during manual validation
must_haves:
  truths:
    - Finder action failures are explicit and user-understandable.
    - Finder behavior is verified against real OS selection cases rather than assumed from unit tests alone.
  artifacts:
    - macos/GotoFinderAction/ActionRequestHandler.swift
    - macos/GotoFinderAction/FinderErrorPresenter.swift
    - README.md
    - macos/GotoTests/FinderErrorPresenterTests.swift
  key_links:
    - Finder error handling is distinct in presentation but consistent with the shared launch outcomes
    - repository documentation explains native prerequisites and Finder validation expectations
---

<objective>
Close the Finder surface with error handling and end-to-end validation.

Purpose: make the Finder action usable in real conditions, including invalid selections and native permission failures.
Output: clear Finder-facing error handling, documentation, and a validated end-to-end Finder launch flow.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-finder-launch-surface/05-CONTEXT.md
@.planning/phases/05-finder-launch-surface/05-RESEARCH.md
@.planning/phases/05-finder-launch-surface/05-VALIDATION.md
@.planning/phases/05-finder-launch-surface/05-01-PLAN.md
@.planning/phases/05-finder-launch-surface/05-02-PLAN.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Finder-specific error presentation for invalid selections and launch failures</name>
  <files>macos/GotoFinderAction/FinderErrorPresenter.swift, macos/GotoFinderAction/ActionRequestHandler.swift</files>
  <action>Implement Finder-facing error messaging for unsupported selections, invalid paths, and shared launcher failures. Keep the wording concrete and actionable so the user understands whether the problem is the selection, the path, or OS permission state.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>Finder failures show clear, surface-appropriate feedback instead of silently failing or dumping raw extension errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused tests for Finder error mapping</name>
  <files>macos/GotoTests/FinderErrorPresenterTests.swift</files>
  <action>Add tests that map common failure conditions into user-facing Finder messages. Cover invalid selection, missing path, permission denial, and unexpected launch failure states.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The Finder error surface is protected against regressions in failure-state mapping.</done>
</task>

<task type="auto">
  <name>Task 3: Document and complete manual Finder validation</name>
  <files>README.md</files>
  <action>Document the Finder action prerequisites, native permission expectations, and the manual validation matrix for normal folder launch, paths with spaces, non-ASCII paths, and invalid selections. Treat this as release-readiness documentation for the Finder surface.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The repository documents how to validate and troubleshoot the Finder action in real macOS usage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] Manual smoke pass confirms normal folder selection works
- [ ] Manual smoke pass confirms invalid selection shows clear feedback
- [ ] Manual smoke pass confirms Finder behavior aligns with the shared Terminal permission model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Finder launch is understandable and debuggable in real user workflows
- The repository has release-ready documentation for the Finder surface
</success_criteria>

<output>
After completion, create `.planning/phases/05-finder-launch-surface/05-03-SUMMARY.md`
</output>
