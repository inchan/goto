---
phase: 05-finder-launch-surface
plan: 01
type: execute
wave: 1
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - macos/Goto.xcodeproj/project.pbxproj
  - macos/GotoFinderAction/ActionRequestHandler.swift
  - macos/GotoFinderAction/Info.plist
  - macos/GotoFinderAction/FinderSelection.swift
  - macos/GotoTests/FinderSelectionTests.swift
autonomous: true
requirements: [FDR-01]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
must_haves:
  truths:
    - A Finder-oriented target exists for the selected-folder launch workflow.
    - Finder selection data can be parsed into a validated folder representation.
    - Finder work stays scoped to action wiring rather than inventing a second app architecture.
  artifacts:
    - macos/Goto.xcodeproj/project.pbxproj
    - macos/GotoFinderAction/ActionRequestHandler.swift
    - macos/GotoFinderAction/Info.plist
    - macos/GotoFinderAction/FinderSelection.swift
    - macos/GotoTests/FinderSelectionTests.swift
  key_links:
    - the Finder target is separate from the menu bar host
    - the Finder target produces validated folder input for the shared launch bridge
---

<objective>
Scaffold the Finder launch surface and lock its selection model.

Purpose: create the Finder-specific target and validate the selected-folder input contract before launch behavior is wired.
Output: a Finder action target, selection parsing helpers, and tests for selection validation.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-finder-launch-surface/05-CONTEXT.md
@.planning/phases/05-finder-launch-surface/05-RESEARCH.md
@.planning/phases/05-finder-launch-surface/05-VALIDATION.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-CONTEXT.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Finder action target scaffold</name>
  <files>macos/Goto.xcodeproj/project.pbxproj, macos/GotoFinderAction/ActionRequestHandler.swift, macos/GotoFinderAction/Info.plist</files>
  <action>Create the Finder-oriented action target and wire the required extension metadata. Keep the target minimal: receive Finder context, parse selected items, and prepare to call the shared launch bridge later. Do not reimplement launch behavior here.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>The repository contains a buildable Finder action target ready to receive selected folders.</done>
</task>

<task type="auto">
  <name>Task 2: Implement selection parsing and validation helpers</name>
  <files>macos/GotoFinderAction/FinderSelection.swift, macos/GotoFinderAction/ActionRequestHandler.swift</files>
  <action>Implement a small abstraction that turns Finder-provided items into a validated folder selection. Reject files, missing paths, and malformed selections early so the launch path gets clean input.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The Finder target can derive a validated folder selection without leaking raw extension plumbing into later launch logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for selection validation</name>
  <files>macos/GotoTests/FinderSelectionTests.swift</files>
  <action>Add tests that cover supported folder selections, rejected file selections, malformed inputs, and basic URL decoding. Keep the tests hermetic and focused on selection derivation rather than Finder UI.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The Finder target has automated coverage for the selected-folder contract before Terminal launch is wired in.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] The Finder target is present and buildable
- [ ] Selection validation rejects non-folder or malformed inputs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a Finder-specific target with a clear selected-folder input contract
- Finder plumbing is separated cleanly from the menu bar host and the Terminal bridge
</success_criteria>

<output>
After completion, create `.planning/phases/05-finder-launch-surface/05-01-SUMMARY.md`
</output>
