---
phase: 05-finder-launch-surface
plan: 02
type: execute
wave: 2
depends_on: ["05-01", "04-03"]
files_modified:
  - macos/GotoFinderAction/ActionRequestHandler.swift
  - macos/GotoFinderAction/FinderLaunchCoordinator.swift
  - macos/Goto/Core/TerminalLauncher.swift
  - macos/GotoTests/FinderLaunchCoordinatorTests.swift
autonomous: true
requirements: [FDR-02, FDR-04]
user_setup:
  - Install full Xcode.app and select it with `xcode-select`
  - Allow Terminal automation when prompted during manual validation
must_haves:
  truths:
    - Finder uses the shared Terminal launch bridge rather than a separate implementation.
    - Finder selection paths survive end-to-end handoff including spaces and non-ASCII characters.
  artifacts:
    - macos/GotoFinderAction/ActionRequestHandler.swift
    - macos/GotoFinderAction/FinderLaunchCoordinator.swift
    - macos/Goto/Core/TerminalLauncher.swift
    - macos/GotoTests/FinderLaunchCoordinatorTests.swift
  key_links:
    - Finder invokes the same Terminal launcher created in Phase 4
    - path encoding and quoting are validated before Terminal launch is attempted
---

<objective>
Wire Finder selection into the shared Terminal launch bridge.

Purpose: make the Finder surface perform the same launch behavior already proven in the menu bar flow.
Output: a Finder launch coordinator that passes validated folder selections into the shared Terminal launcher without path corruption.
</objective>

<execution_context>
@/Users/inchan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/inchan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-finder-launch-surface/05-CONTEXT.md
@.planning/phases/05-finder-launch-surface/05-RESEARCH.md
@.planning/phases/05-finder-launch-surface/05-VALIDATION.md
@.planning/phases/05-finder-launch-surface/05-01-PLAN.md
@.planning/phases/04-native-macos-host-and-menu-bar/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Finder launch coordinator that reuses the shared launcher</name>
  <files>macos/GotoFinderAction/FinderLaunchCoordinator.swift, macos/GotoFinderAction/ActionRequestHandler.swift</files>
  <action>Create a thin coordination layer that takes a validated Finder folder selection and invokes the shared Terminal launch bridge from Phase 4. Keep Finder-specific concerns limited to selection and extension lifecycle handling.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build</verify>
  <done>The Finder target launches through the same Terminal bridge used by the menu bar surface.</done>
</task>

<task type="auto">
  <name>Task 2: Harden path handoff for spaces and non-ASCII characters</name>
  <files>macos/GotoFinderAction/FinderLaunchCoordinator.swift, macos/Goto/Core/TerminalLauncher.swift</files>
  <action>Ensure the Finder-to-Terminal flow preserves path fidelity for spaces and non-ASCII characters. Adjust the shared launcher only where necessary so the change benefits both surfaces without breaking menu bar behavior.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>Finder launch requests preserve path correctness across the full handoff into Terminal.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for coordinated Finder launch behavior</name>
  <files>macos/GotoTests/FinderLaunchCoordinatorTests.swift</files>
  <action>Add tests that cover valid folder launch requests, rejected invalid inputs, and serialized launch payloads for paths with spaces and non-ASCII characters. Mock or isolate the actual Terminal side effect where needed.</action>
  <verify>xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test</verify>
  <done>The Finder launch coordination layer has automated protection against path corruption and launch drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' build`
- [ ] `xcodebuild -project macos/Goto.xcodeproj -scheme Goto -destination 'platform=macOS' test`
- [ ] Manual smoke pass confirms folders with spaces open correctly
- [ ] Manual smoke pass confirms folders with non-ASCII characters open correctly
- [ ] Finder still reuses the shared Terminal launch bridge rather than a second launch implementation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Finder launch behavior is consistent with the menu bar launch flow
- Path integrity is preserved across the Finder-to-Terminal handoff
</success_criteria>

<output>
After completion, create `.planning/phases/05-finder-launch-surface/05-02-SUMMARY.md`
</output>
